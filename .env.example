HOST=0.0.0.0
PORT=8080
# حتماً مقدار تصادفی قوی بگذارید (مثلاً: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=change_me_to_a_long_random_hex
DATABASE_URL=postgres://postgres:YOUR_PASSWORD@localhost:5432/bfg_cmms
# برای دمو/تست بدون PostgreSQL (داده‌ها موقتی در حافظه): 
# PG_MEM=1
